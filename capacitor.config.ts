import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hello_word_ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
